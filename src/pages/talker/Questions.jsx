import {Text, View} from 'react-native';
import {styles} from '../../styles/styles';

/**
 * Método para renderizar página de Preguntas.
 * @return {JSX.Element}
 */
export function Questions() {
  return (
    <View style={styles.blank_background}>
      <Text>Preguntador</Text>
    </View>
  );
}
